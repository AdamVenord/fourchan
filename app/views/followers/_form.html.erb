<h1><%= @follower.id ? 'Edit Follower' : 'Create Follower' %></h1>
<hr />
<%= form_for @follower do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %> 